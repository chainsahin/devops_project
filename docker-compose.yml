web-app-green:
    image: ${DOCKERHUB_USERNAME}/devops_project:${GITHUB_RUN_NUMBER} # Yeni sürüm etiketi 🏷️
    ports:
      - "____:5000" # Yeni kapı numarası 🚪
    environment:
      - REDIS_HOST=redis-server
      - REDIS_PORT=6379
    depends_on:
      - redis-server

  redis-server:      # 1. Seviye
    image: "redis:alpine"
    volumes:
      - redis_data:/data

volumes:             # 0. Seviye (En solda)
  redis_data:
