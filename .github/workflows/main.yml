name: DevOps & Cloud CI-CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  # 1. BÖLÜM: Docker İmajını Oluşturma ve Docker Hub'a Gönderme
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu Çek
        uses: actions/checkout@v4

      - name: Docker Hub'a Login Ol
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker İmajını İnşa Et ve Pushla
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/devops_project:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/devops_project:${{ github.run_number }}

  # 2. BÖLÜM: Sunucuya Akıllı Dağıtım (Deploy)
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Kodu Çek
        uses: actions/checkout@v4

      # docker-compose.yml ve nginx.conf dosyalarını sunucuya gönderir
      - name: Dosyaları Sunucuya Gönder (SCP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "docker-compose.yml,nginx.conf"
          target: "/root/devops_project"

      - name: Sunucuya SSH ile Bağlan ve Güncelle
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          # Robotun sunucuda değişkenleri tanımasını sağlar
          envs: GITHUB_RUN_NUMBER,DOCKERHUB_USERNAME
          script: |
            cd /root/devops_project
            
            # Eğer Docker yanlışlıkla nginx.conf klasörü oluşturduysa temizle
            if [ -d "nginx.conf" ]; then rm -rf nginx.conf; fi
            
            # Değişkenleri sunucu ortamına aktar
            export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
            export GITHUB_RUN_NUMBER=${{ github.run_number }}
            
            # Sadece değişen imajları çek ve konteynerları güncelle
            docker compose pull
            docker compose up -d
