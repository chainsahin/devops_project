services:
  # Mavi Sürüm (Mevcut Canlı) 🔵
  web-app-blue:
    image: ${{ secrets.DOCKERHUB_USERNAME }}/devops_project:latest
    ports:
      - "8081:5000"
    environment:
      - REDIS_HOST=redis-server
    depends_on:
      - redis-server

  # Yeşil Sürüm (Yeni Gelecek) 🟢
  web-app-green:
    image: ${{ secrets.DOCKERHUB_USERNAME }}/devops_project:${{ github.run_number }}
    ports:
      - "8082:5000"
    environment:
      - REDIS_HOST=redis-server
    depends_on:
      - redis-server

  # Trafik Polisi 👮‍♂️
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web-app-blue
      - web-app-green

  # Veritabanı 🗄️
  redis-server:
    image: "redis:alpine"
